const getCounts = (entryData) => {
  console.log(entryData);
  //   if (entryData) {
  //     const counts = [
  //       {
  //         fruit: "برتقال",
  //         result: `${entryData.filter((el) => el.fruitName === "برتقال").length}`,
  //       },
  //       {
  //         fruit: "ليمون",
  //         result: `${entryData.filter((el) => el.fruitName === "ليمون").length}`,
  //       },
  //       {
  //         fruit: "عنب",
  //         result: `${entryData.filter((el) => el.fruitName === "عنب").length}`,
  //       },
  //       {
  //         fruit: "تفاح",
  //         result: `${entryData.filter((el) => el.fruitName === "تفاح").length}`,
  //       },
  //       {
  //         fruit: "فريز",
  //         result: `${entryData.filter((el) => el.fruitName === "فريز").length}`,
  //       },
  //       {
  //         fruit: "منجو",
  //         result: `${entryData.filter((el) => el.fruitName === "منجو").length}`,
  //       },
  //       {
  //         fruit: "بطيخ",
  //         result: `${entryData.filter((el) => el.fruitName === "بطيخ").length}`,
  //       },
  //       {
  //         fruit: "كرز",
  //         result: `${entryData.filter((el) => el.fruitName === "كرز").length}`,
  //       },
  //     ];

  //     return counts;
  //   }
};

export default getCounts;
